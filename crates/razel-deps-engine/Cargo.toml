[package]
name = "razel-deps-engine"
version = "0.1.0"
edition.workspace = true
license.workspace = true
rust-version.workspace = true
publish = false

[dependencies]
razel-loading = { version = "0.1.0", path = "../razel-loading" }
razel-wire = { version = "0.1.0", path = "../razel-wire" }
razel-core = { version = "0.1.0", path = "../razel-core" }
razel-dds = { version = "0.1.0", path = "../razel-dds" }

[dev-dependencies]
razel-wire = { version = "0.1.0", path = "../razel-wire" }
