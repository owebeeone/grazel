# Host stub (round 43): apple_support's universal_binary — grpc's build system loads it
# unconditionally; declarations absorb (nothing in the TF cone deps the universal outputs).
universal_binary = razel_host_absorb
