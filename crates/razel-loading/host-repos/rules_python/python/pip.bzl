# Host stub (fetch R1): @rules_python pip surface. pip_parse generates @pypi — recorded as
# a repo spec via the recorder pattern is a LATER rung (the stub-hub posture, round 25);
# extraction-time these are structured no-ops.
def package_annotation(**kwargs):
    return kwargs

def pip_parse(name = None, **kwargs):
    pass
