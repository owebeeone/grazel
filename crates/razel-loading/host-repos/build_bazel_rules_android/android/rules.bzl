# Host stub (fetch R4): rules_android surface upstream BUILDs load (flatbuffers, TF's
# flatbuffers overlay build_defs). Declarations absorb — nothing in the TF cone deps them.
android_library = razel_host_absorb
android_binary = razel_host_absorb
aar_import = razel_host_absorb
