[package]
name = "grazel-cli-lib"
version = "0.1.0"
edition.workspace = true
license.workspace = true
rust-version.workspace = true
publish = false

[dependencies]
grazel-node = { version = "0.1.0", path = "../grazel-node" }
razel-cli = { version = "0.1.0", path = "../razel-cli" }
razel-daemon = { version = "0.1.0", path = "../razel-daemon" }
razel-wire = { version = "0.1.0", path = "../razel-wire" }

[dev-dependencies]
tempfile = "3.27.0"
