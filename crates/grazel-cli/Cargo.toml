[package]
name = "grazel"
version = "0.1.0"
edition.workspace = true
license.workspace = true
rust-version.workspace = true
repository.workspace = true
publish = false

[package.metadata.dist]
dist = true

[[bin]]
name = "grazel"
path = "src/main.rs"

[dependencies]
grazel-cli-lib = { version = "0.1.0", path = "../grazel-cli-lib" }

[dev-dependencies]
tempfile = "3.27.0"
